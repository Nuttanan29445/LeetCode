def findAndReplacePattern(words, pattern):
    print("".join(set(pattern)))
        # for i in words:
        #     x1 = list(set(k for k in i) )
        #     print(x1)
        #     if len(x1) == len(x):
        #         for index,j in enumerate(x1): 
        #             y = i.replace(j,x[index])
        #             print(y)
        
                    
                     
findAndReplacePattern(["aqq","mee"],"abb")